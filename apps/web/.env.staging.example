# Staging — copy to .env.staging (not loaded by Next.js; use for reference or inject into Wrangler).
# Set these in Cloudflare Dashboard → Workers → aistroyka-web-staging → Settings → Variables.

NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
NEXT_PUBLIC_APP_URL=
