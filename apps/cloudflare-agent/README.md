# Cloudflare Agent (legacy / not in product contour)

This directory previously held an experimental Cloudflare Workers agent scaffold. **No application source remains** — only local `node_modules` and `.wrangler` cache (gitignored).

**Canonical web runtime:** `apps/web` (OpenNext + Wrangler, see repo root `README.md`).

Do not provision or deploy from this path unless restoring from `docs/_archive/` with an explicit product decision.
