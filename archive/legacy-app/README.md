# Legacy root app (do not use for build/deploy)

This directory was the original root-level Next.js `app` folder. The canonical application is **apps/web**. Build and deploy use `apps/web` only; this folder is kept for reference and must not be used by any build tooling.

- **Moved:** Phase 2 SaaS Core (see docs/REPORT-PHASE2-SAAS-CORE.md).
- **Do not** reference this path from next.config, tsconfig, or CI.
