# Supabase live snapshot — BEFORE hardening

**Timestamp:** 20260303185654  
**Project ref:** vthfrxehrursfloevnlp (AISTROYKA, ACTIVE_HEALTHY, eu-central-1)

---

## A) Views in schema public (25 total)

All owner: postgres. (Postgres views do not have SECURITY DEFINER; they execute with owner privileges.)

| view_name |
|-----------|
| active_analysis_jobs, active_tenants, arpu, dead_letter_jobs, failure_metrics, failure_trend |
| job_durations, job_metrics, job_throughput, job_trace |
| monthly_ai_cost, monthly_churn, monthly_margin, monthly_revenue |
| plan_monthly_revenue, queue_latency, sla_breaches, sla_metrics |
| tenant_active_jobs, tenant_dynamic_pricing, tenant_hourly_usage, tenant_monthly_usage, tenant_usage_summary |
| worker_metrics, worker_status |

---

## B) View grants (anon, authenticated, service_role)

**All 25 views:** anon_select=true, authenticated_select=true, service_role_select=true.

→ **HIGH RISK:** Admin/observability views are readable by anon and authenticated.

---

## C) Tables in public with RLS disabled

| table_name    | rls_enabled |
|---------------|-------------|
| **job_sweep_log** | **false** |
| (all other tables) | true |

---

## D) job_sweep_log

- rls_enabled: **false**
- anon_select: **true**
- auth_select: **true**
- service_role_select: true

→ **HIGH RISK:** Operational log exposed to anon/authenticated.

---

## E) SECURITY DEFINER functions (public schema)

All have: anon_execute=true, authenticated_execute=true, service_role_execute=true.

| function_name | args |
|---------------|------|
| check_tenant_quota | p_tenant_id uuid |
| claim_job_execution | p_job_id uuid, p_worker_id uuid |
| complete_analysis_job | (2 overloads) |
| create_analysis_job | p_tenant_id uuid, p_media_id uuid, p_priority text |
| dequeue_job | (3 overloads) |
| generate_monthly_invoice | p_tenant_id uuid, p_month timestamptz |
| increment_worker_processed | p_worker_id text |
| issue_invoice | p_snapshot_id uuid |
| mark_invoice_paid | p_snapshot_id uuid |
| mark_overdue_invoices | (no args) |
| mark_stale_jobs_failed | p_timeout_minutes integer |
| pick_next_analysis_job | (2 overloads) |
| process_stripe_payment | (many args) |
| reassign_queued_jobs_from_inactive_regions | (no args) |
| record_job_event | p_job_id uuid, p_worker_id uuid, p_event_type text, p_payload jsonb |
| recover_dead_workers | p_timeout_seconds integer |
| retry_job | p_job_id uuid, p_error text, p_max_retries integer |
| worker_heartbeat | p_worker_id uuid |

→ **CRITICAL:** All SECURITY DEFINER functions executable by anon and authenticated.

---

*No changes applied in this step.*
