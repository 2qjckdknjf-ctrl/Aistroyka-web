# docs/tasks/

One file per active task, created from `docs/ops/TASK_TEMPLATE.md`.

Naming: `<branch-type>-<short-topic>.md` (e.g. `ops-cloud-agent-bootstrap.md`, `fix-sync-409-cursor.md`).

A task file is opened when work starts and closed (or paired with a handoff) when work pauses or completes.
