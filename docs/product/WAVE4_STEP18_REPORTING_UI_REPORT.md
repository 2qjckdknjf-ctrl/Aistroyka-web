# Wave 4 Step 18 — Review pack UI

## D1 — Surfaces

| Surface | Location |
|---------|----------|
| `ProjectReviewPackPanel` | Manager project dashboard (`DashboardProjectDetailClient`) — below KPI grid |
| `PortfolioReviewPackSection` | Portfolio page — below portfolio control table, above legacy `PortfolioOverview` |

## D2 — UX

- Compact cards: executive signal badge, summary line, budget / handover / aftercare / action focus.  
- Links drill to project tabs (`?tab=costs`, `?tab=aftercare`) and project overview.  
- Portfolio pack lists critical and attention projects with primary reason + link.  

## D3 — Scope

No shell redesign; no print/PDF export in this step.  

## Limitations

- Panel copy is English in components.  
- Blocker detail list on project pack is summarized (not every blocker expanded in UI).  
