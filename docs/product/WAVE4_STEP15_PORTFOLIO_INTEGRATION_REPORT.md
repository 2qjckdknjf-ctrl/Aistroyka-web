# Wave 4 Step 15 — Integration

## Truth sources

- **Project summary** — same pipeline as `GET /api/v1/projects/:id/summary` aggregates.  
- **Handover readiness** — same blocker definitions as Wave 4 Step 12 (`computeHandoverReadinessFromSummary`).  
- **Aftercare** — Wave 4 Step 14 open request count.

## Drilldowns

- Links target **existing** manager surfaces (`/dashboard/projects/:id` with tabs, client sub-routes, approvals queue).

## Intentionally not changed

- `/api/v1/portfolio/summary` (AI / risk / evidence intelligence) — kept separate.  
- Project detail layout and tabs.  
- Stakeholder portfolio routes.
