# Wave 4 Step 9 — Stakeholder activity / timeline (summary)

**Goal:** A **unified, audience-shaped activity timeline** for client/portal transparency, distinct from team operations (`/timeline`) and from Step 8 delivery/notifications.

**Implementation anchors:**

- `stakeholder-activity-timeline.types.ts` / `stakeholder-activity-timeline.repository.ts`
- `GET /api/v1/projects/:id/stakeholder-activity`
- UI: `StakeholderActivityBlock`, `ClientPortalActivitySection`, Activity tab in `DashboardProjectDetailClient`

**Docs index:** `WAVE4_STEP9_STAKEHOLDER_TIMELINE_*.md` in `docs/product/`.
