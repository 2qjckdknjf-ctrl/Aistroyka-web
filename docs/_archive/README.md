# Archive (Release 1)

- `packages-contracts-openapi/` — OpenAPI generator (Zod → `dist/openapi.json`). Regenerate: `bash scripts/generate-openapi.sh` from repo root.
- `audit_prod_smoke_green_v4/` — historical pilot smoke artifacts (not source code).
