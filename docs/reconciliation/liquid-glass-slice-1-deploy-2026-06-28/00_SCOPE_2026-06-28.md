# Liquid Glass Slice 1 — Production Deploy Evidence — Scope

Date: 2026-06-28

## Deployed main

- SHA: `c69bd40bb84968a2a47196112cd76ca0b13d8ad1`
- short sha7 (health format, 7 chars): `c69bd40`

## Deploy scope

- Deployed current `main` (Liquid Glass public shell Slice 1, merged via PR #151) to Cloudflare production worker `aistroyka-web-production` serving `https://aistroyka.ai`.
- Deploy executed through the **canonical CI pipeline**, not a local manual deploy:
  - `Deploy Cloudflare (Staging)` on push to `main` → success
  - `Deploy Cloudflare (Production)` via `workflow_run` on staging success → success
- No Cloudflare deploy credentials are configured locally; a manual `wrangler deploy` was intentionally NOT used (would bypass the gated pipeline + post-deploy smoke).

## Safety boundaries honored

- No code changes.
- No i18n changes.
- No mobile changes.
- No backend/API/auth/RBAC changes.
- No workflow/script/env changes.
- No migrations applied.
- No live Supabase data touched.
- No branch protection changes.
- Non-mutating smoke only (read-only HTTP GETs; no login, no writes, no uploads).

## Claims discipline

- "Liquid Glass live" claimed ONLY after buildStamp match + live markers > 0 (both verified — see docs 03/04).
- No production GA claim.
- Mobile readiness unchanged.
