# Android Manager Instrumented Launch Smoke — Scope (2026-06-28)

## Base main SHA

`4850de1f8f334582ce992bc8b254df726ca5f33d`

## Scope

Add the missing **Android Manager instrumented launch smoke** for issue #112, closing the Android Manager instrumented gap left after PR #148 (which covered the Worker instrumented launch).

## Files changed

| File | Type | Reason |
|------|------|--------|
| `android/AiStroykaManager/src/androidTest/java/ai/aistroyka/manager/ManagerAppLaunchInstrumentedTest.kt` | New test | Manager launch smoke (mirrors Worker) |
| `android/AiStroykaManager/build.gradle.kts` | Minimal, justified config | Manager module had **no** `testInstrumentationRunner` and **no** `androidTest`/`ui-test-manifest` dependencies; the instrumented test cannot compile or run without them. Mirrors the exact setup already present in `android/AiStroykaWorker/build.gradle.kts`. No production source/logic changed. |

> Justification note: the `build.gradle.kts` change is the minimum required to make an instrumented test compile and execute, exactly as the Worker module already does. It adds only `testInstrumentationRunner`, `androidTestImplementation` test libs, and `debugImplementation(ui-test-manifest)`. No app/runtime code, app behavior, signing, or dependency of the shipped APK changed.

## Explicit exclusions

- No deploy, no Google Play upload, no release signing
- No migrations, no live Supabase data
- No iOS / web / backend / workflow / script / env / lockfile changes
- No full Android readiness, full mobile readiness, pilot-live, or production GA claims

## Prior merged evidence (context)

- **PR #146:** iOS simulator build + login-surface smoke PASS (partial)
- **PR #148:** Android debug assemble + shared tests + Worker instrumented launch PASS (partial)
- **PR #154:** iOS Layer B staging E2E PASS 3/3 (partial)
