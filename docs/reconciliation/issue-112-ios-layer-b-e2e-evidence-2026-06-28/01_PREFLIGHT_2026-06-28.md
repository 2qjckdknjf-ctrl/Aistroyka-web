# iOS Layer B Live E2E — Preflight (2026-06-28)

## Workflows / scripts inspected

| Artifact | Purpose |
|----------|---------|
| `.github/workflows/ios-e2e-integration.yml` | Layer B CI workflow (`workflow_dispatch` only; requires repo secrets) |
| `.github/workflows/ios-ui-smoke.yml` | Layer A simulator smoke (not run in this slice) |
| `ios/scripts/run-ios-e2e-integration-local.sh` | **Official local Layer B runner** (used) |
| `ios/scripts/ci-pick-iphone-simulator-udid.sh` | Simulator UDID picker (used) |
| `scripts/smoke/ios_mobile_api_chain.sh` | Mobile API chain preflight (used against staging) |
| `docs/reconciliation/issue-112-mobile-build-evidence-plan-2026-06-26/01_IOS_EVIDENCE_REQUIREMENTS_2026-06-26.md` | Requirements reference |
| `docs/reconciliation/issue-112-ios-simulator-evidence-2026-06-26/**` | Prior Layer A evidence |

## Xcode

- **Version:** Xcode 26.6 (Build 17F113)

## Projects / schemes

| Project | Scheme | UITest target |
|---------|--------|---------------|
| `ios/AiStroykaManager/AiStroykaManager.xcodeproj` | `AiStroykaManager` | `AiStroykaManagerUITests` |
| `ios/AiStroykaWorker/AiStroykaWorker.xcodeproj` | `AiStroykaWorker` | `AiStroykaWorkerUITests` |

## Simulator / device

- **Picker:** `ios/scripts/ci-pick-iphone-simulator-udid.sh`
- **Selected:** iPhone 17 Pro, iOS 26.5
- **UDID:** `779B2896-9F65-4CD6-93FB-B791DAAE02A8`
- **Destination:** `platform=iOS Simulator,id=779B2896-9F65-4CD6-93FB-B791DAAE02A8`

## Required env / config (presence only — values NOT recorded)

| Name / file | Status |
|-------------|--------|
| `ios/Config/Secrets.xcconfig` | PRESENT (BASE_URL, SUPABASE_URL, SUPABASE_ANON_KEY keys set) |
| `.env.pilot` | PRESENT |
| `apps/web/.env.local` | PRESENT |
| `SMOKE_EMAIL` | PRESENT |
| `SMOKE_PASSWORD` | PRESENT |
| `NEXT_PUBLIC_SUPABASE_URL` | PRESENT |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | PRESENT |
| `IOS_E2E_BASE_URL` | Set at runtime to `https://staging.aistroyka.ai` |
| `CI_SIGNING_HACK` | Set to `1` for ad-hoc simulator signing |

**Not required locally (CI-only names):** `PILOT_E2E_EMAIL`, `PILOT_E2E_PASSWORD`, `NEXT_PUBLIC_SUPABASE_URL_PRODUCTION`, `NEXT_PUBLIC_SUPABASE_ANON_KEY_PRODUCTION` — local run used `.env.pilot` + `apps/web/.env.local` equivalents per script fallback chain.

## Execution mode

**LOCAL_E2E**

Reason: Official script exists; pilot credentials and Supabase public config present locally; staging target explicitly set; mobile API chain preflight PASS before UITests.

## Staging API preflight (before UITests)

Command:

```bash
BASE_URL=https://staging.aistroyka.ai bash scripts/smoke/ios_mobile_api_chain.sh
```

Result: **PASS** — worker create+sync, manager me+reports+intelligence against staging.
