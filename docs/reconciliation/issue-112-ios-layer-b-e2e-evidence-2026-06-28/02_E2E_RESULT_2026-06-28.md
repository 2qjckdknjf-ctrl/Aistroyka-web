# iOS Layer B Live E2E — Result (2026-06-28)

## Command used

```bash
export CI_SIGNING_HACK=1
export IOS_E2E_BASE_URL="https://staging.aistroyka.ai"
bash ios/scripts/run-ios-e2e-integration-local.sh
```

Credentials loaded from repo-root `.env.pilot` and `apps/web/.env.local` per script (values not recorded).

## Target environment

**Staging pilot** — `https://staging.aistroyka.ai`

- Not production (`aistroyka.ai`) despite script default.
- Tests exercise live tenant APIs on staging; Worker report draft flow may create staging pilot data (expected Layer B behavior).

## Auto-pinned project

Script resolved `IOS_E2E_PROJECT_ID` from staging API (first project in tenant list). Project id present in local log; not repeated here to avoid unnecessary tenant detail in docs.

## Result

**PASS** (exit 0)

Wall time: ~105 seconds (build-for-testing + 3 UITests).

## Tests executed

| # | App | Test | Duration | Result |
|---|-----|------|----------|--------|
| 1 | Manager | `ManagerSmokeUITests/testManager_livePilot_projectIntelligenceAndCopilot` | 47.1s | PASS |
| 2 | Manager | `ManagerSmokeUITests/testManager_livePilot_loginAndReachReportsInbox` | 10.6s | PASS |
| 3 | Worker | `WorkerSmokeUITests/testWorker_livePilot_loginAndOpenNewReportDraft` | 12.9s | PASS |

**Total:** 3/3 PASS, 0 failures.

## Preflight inside script

- Manager `GET /api/v1/me` on staging: **200 OK**
- Worker `GET /api/v1/me` on staging: **200 OK**

## Log location (local, not committed)

`evidence/ios-layer-b-e2e-2026-06-28/logs/ios-layer-b-e2e.log` (gitignored path under local `evidence/`; contains operational email reference only, no passwords)

## Sanitized excerpt

```
Preflight manager /api/v1/me OK on https://staging.aistroyka.ai
Preflight worker /api/v1/me OK on https://staging.aistroyka.ai
testManager_livePilot_projectIntelligenceAndCopilot ... passed (47.132 seconds)
testManager_livePilot_loginAndReachReportsInbox ... passed (10.587 seconds)
testWorker_livePilot_loginAndOpenNewReportDraft ... passed (12.884 seconds)
OK — Manager + Worker live pilot E2E UITests passed (intelligence/copilot + report draft).
```

## Failure summary

None.
