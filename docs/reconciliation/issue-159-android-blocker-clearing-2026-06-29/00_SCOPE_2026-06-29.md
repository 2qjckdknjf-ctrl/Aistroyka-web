# Android Distribution Blocker-Clearing — Scope

- **Issue:** [#159 — Android distribution readiness preflight: Google Play](https://github.com/2qjckdknjf-ctrl/Aistroyka-web/issues/159)
- **Date:** 2026-06-29
- **Base main SHA:** `ac91a9eafbfb3ba5bede0800809d7b67694b55c3`
- **Branch:** `fix/android-distribution-readiness-issue-159`

## Goal

Clear the **local/code-side** Android distribution blockers recorded in the
preflight (PR #162 / issue #159):

1. Bump `compileSdk` / `targetSdk` to **35** (Google Play target API requirement).
2. Wire a **release `signingConfig`** safely from the local, untracked
   `android/keystore.properties` (no secrets committed).
3. Add a **safe `versionCode` override** strategy (no more hard-coded `1` for store builds).
4. Prove **signed release AAB/APK build readiness** locally (secrets present in this env).

## Non-goals / not performed

- No upload to Google Play; no `publish`/`supply` tasks; no Play Console mutation.
- No secrets committed (keystore, `keystore.properties`, service account JSON).
- No APK/AAB/keystore artifacts committed (all gitignored).
- No deploy, migrations, live Supabase data, or env changes.
- No iOS changes. No web/app code, workflow, or package/lockfile changes.
- No Google Play readiness / pilot-live / production-GA claim.

## Store-side blockers still owned by the owner (out of code scope)

- Play Console app records / access, store listing, Data safety form, privacy policy,
  content rating, screenshots, internal-testing-track signed-AAB upload evidence.

These remain `OWNER_ACTION_REQUIRED`; issue #159 stays **OPEN** after this PR.
