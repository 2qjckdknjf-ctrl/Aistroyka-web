# Liquid Glass — Slice 1 — Diff Scope

Date: 2026-06-28

## Summary

41 source files changed under `apps/web/` (30 added, 11 modified). No forbidden files.

## Added — Liquid Glass foundation (web-only)

- `apps/web/lib/design/liquid-glass.ts` — foundation helper (intensity/tints); required by foundation components.
- `apps/web/components/design/liquid-glass/` — `AppGlassRoot`, `GlassButton`, `GlassHeroCard`, `GlassIntensityControl`, `GlassLink`, `GlassNav`, `GlassPanel`, `GlassSurface`, `LiquidGlass`, `LiquidGlassFilter`, `index.ts`.
- `apps/web/styles/liquid-glass.css` — LG design tokens / utility CSS (`--lg-*`).
- `apps/web/public/effects/glass-filter.svg` — SVG displacement filter asset.

## Added — Public shell + homepage components

- `apps/web/components/public/` — `PublicAmbientField`, `PublicLiquidGlassRoot`, `PublicHeroCTA`, `PublicHeroLens`, `PublicHeroMetrics`, `PublicRevealGlassCard`, `CountUpText`, `PublicCTASection`, `PublicRelatedLinksSection`, `PublicPageHero`, `PublicFeatureGrid`, `PublicFeatureCard`, `PublicProofSection`, `PublicTimelineSection`, `PublicGlassContentPage`, `PublicGlassShells`, `PublicJsonLd`.
  - These form the cohesive public component set re-exported by the barrel; bringing the full set avoids a broken barrel and keeps the slice self-consistent. Components consumed directly by the shell/homepage in this slice: header/footer, ambient field, LG root, hero CTA/lens/metrics, reveal card, related links, CTA section.

## Modified — app shell (additive)

- `apps/web/app/layout.tsx` — mounts `<AppGlassRoot />` (2-line additive change).
- `apps/web/app/globals.css` — adds `@import "../styles/liquid-glass.css"` and restyles existing utility classes (`btn-primary`, `btn-secondary`, `card`, `card-elevated`, `input-field`) + adds `surface-glass*` + `public-ambient-field*` classes. Additive/restyle only; no token deletion.
- `apps/web/app/[locale]/(public)/layout.tsx` — wraps the public shell with `PublicAmbientField` + `PublicLiquidGlassRoot` + glass header/footer; adds WebSite JSON-LD using `public.layout` keys.
- `apps/web/app/[locale]/(public)/PublicHomeContent.tsx` — LG hero/lens + glass sections. **`MOCK_METRICS` removed**; metrics now render truthful qualitative `public.homeMetrics` labels (no fake numbers).
- `apps/web/components/public/{PublicHeader,PublicFooter}.tsx` — glass nav/footer; `index.ts` barrel extended (superset; existing exports preserved).

## Modified — i18n (additive only)

- `apps/web/messages/{en,ru,es,it}.json` — **+45 leaf keys per locale**, restricted to namespaces `public.{home,homeMetrics,cta,nav,footer,layout}`. Existing keys preserved (no copy regression, no CTA regression). Includes the pilot-first `public.cta` block.

## Confirmation — no forbidden changes

- No `apps/web/app/api/**`, no `middleware.ts`.
- No `lib/supabase/**`, `lib/platform-owner/**`, `lib/auth/**`, `supabase/**`, `migrations/**`.
- No `.github/workflows/**`, `scripts/**`, env files, deploy config.
- No `package.json` / `package-lock.json` / `bun.lock*` changes (no tooling reverts).
- No `ios/**`, no `android/**`.
- No PR #148 files.

(Pre-existing untracked working-tree files — `android/.secrets/`, `android/keystore.properties`, `docs/web/` — are NOT part of this PR and were not staged.)
