# Liquid Glass — Slice 1 (Public Shell) — Scope

Date: 2026-06-28
Branch: `feat/liquid-glass-public-slice-1-2026-06-28`

## Base / Source

- Base main SHA: `3b49865df597b1a71ee0a409dfe138cbe6f70eba` (branched from `origin/main`)
- Re-slice source: `origin/release/web-pilot-rc` (web-only LG branch, stale; used as file source only — NO merge)
- Reference plan: `docs/reconciliation/liquid-glass-fresh-recon-2026-06-28/06_NEXT_EXECUTION_PROMPT_2026-06-28.md` (Slice 1)

## Goal

Re-apply only the approved Slice 1 Liquid Glass public shell allowlist onto fresh `main`:
- Liquid Glass foundation (web-only)
- Public shell / layout / root / ambient field
- Public homepage hero/lens integration
- Minimal additive global CSS hook
- Minimal i18n keys directly required by the public shell + homepage

## Allowlist (applied)

- `apps/web/components/design/liquid-glass/**` (foundation components)
- `apps/web/lib/design/liquid-glass.ts` (foundation helper required by the components)
- `apps/web/styles/liquid-glass.css`
- `apps/web/public/effects/glass-filter.svg`
- `apps/web/app/layout.tsx` (additive `AppGlassRoot` mount)
- `apps/web/app/globals.css` (additive `@import` + glass restyle of existing utility classes)
- `apps/web/app/[locale]/(public)/layout.tsx` (public shell: ambient field + LG root + glass header/footer)
- `apps/web/app/[locale]/(public)/PublicHomeContent.tsx` (LG home hero/lens — fake metrics removed)
- `apps/web/components/public/**` (public shell + homepage components + barrel)
- `apps/web/messages/{en,ru,es,it}.json` (additive keys only, namespaces: `public.{home,homeMetrics,cta,nav,footer,layout}`)

## Forbidden areas (NOT touched)

- `apps/web/app/api/**`, `middleware.ts`
- `lib/supabase/**`, `lib/platform-owner/**`, `lib/auth/**`, `supabase/**`, `migrations/**`
- `.github/workflows/**`, `scripts/**`, env files, deploy config
- `package.json`, `package-lock.json`, `bun.lock*` (no tooling reverts)
- `ios/**`, `android/**`

## Hard constraints honored

- No broad branch merge (targeted `git checkout <path>` only).
- No deploy. No mutating smoke. No live Supabase data.
- No production GA / "latest main deployed" / "Liquid Glass live" claims.
- PR #148 (Android evidence) untouched and kept separate.
- Pilot-first CTAs preserved; no fake numeric production metrics.
