# 00 — Scope (Liquid Glass Fresh Reconnaissance)

**Date:** 2026-06-28  
**Operator:** Liquid Glass Fresh Reconnaissance Operator (read-only / docs-only)  
**Branch:** `docs/liquid-glass-fresh-recon-2026-06-28`  
**Base main SHA:** `d54278c680162cf8af598466fda1d72dc9c733dc`

---

## Why this recon is required before shipping Liquid Glass

PR #149 established (runtime-first) that production is **aligned with main**
(`buildStamp.sha7 = bc992b7`) and that the **absence of Liquid Glass on
`aistroyka.ai` is because LG is not in `main`** — not a deploy lag. The LG work
lives only on unmerged design branches that are now **80+ commits behind main**.

Before any LG shipment, we must answer: which branch is the safest source, how
stale is it, what dangerous areas does it touch, and what is the minimal first
slice that can land via a normal protected PR.

## Scope (this task)

- **Read-only** branch/diff/file inspection
- **Docs/evidence-only** output
- Local validation (lint/test/build) on the docs branch
- One PR opened (not merged)

## Hard constraints honored

- **No merge** of any LG/design branch
- **No public UI code change**
- **No deploy**, no mutating smoke, no migrations, no live data
- **No env / workflow / branch-protection change**
- **No claim** that Liquid Glass is live or that production is GA
- **No touch** of `release/mobile-pilot-rc` or `cursor/aistroyka-system-maturity-7957`
- PR #148 (Android evidence) kept **separate** from LG work

## Allowed outputs

- `docs/reconciliation/liquid-glass-fresh-recon-2026-06-28/**`
- (Not used) optional one-line `docs/CURRENT_PROJECT_TRUTH_INDEX.md` link
