# Manual smoke — AiStroyka Manager (staging / pilot)

Use with a **pilot** manager (`owner` / `admin` / `member`) and matching `BASE_URL`.  
Full matrix: `docs/mobile-ios/IOS_E2E_VALIDATION_REPORT.md` (Layer B).

## Environment (record)

| Field | Value |
|--------|--------|
| Date | |
| Git SHA / build | |
| `BASE_URL` | |
| Device / OS | |

## Checklist

- [ ] Cold launch → onboarding or login.
- [ ] Home dashboard loads; help hints / assistant block (optional empty if API down).
- [ ] Projects → open project; tasks list sanity.
- [ ] Reports inbox: open submitted report; **evidence** thumbnails load or show localized failure.
- [ ] Review: approve **or** request changes / reject with **non-empty** note where required.
- [ ] AI tab: list loads or empty state (no crash).
- [ ] Sign out / session refresh sanity.

## Notes

```
(pastes, screenshots path, ticket id)
```
