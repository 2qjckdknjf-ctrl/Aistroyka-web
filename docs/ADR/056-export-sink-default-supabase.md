# ADR-056: Default export sink Supabase table

**Status:** Accepted  
**Decision:** Default sink for export jobs is supabase_table (write to export_rows). External sinks (S3, BigQuery, Snowflake) are stubs until credentials and implementation added.
