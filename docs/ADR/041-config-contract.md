# ADR-041: Config payload contract

**Status:** Accepted  
**Decision:** GET /api/v1/config response validated by ConfigPayloadSchema (flags, serverTime, traceId, clientProfile). Optional limits key for future. OpenAPI documents ConfigPayload.

**Context:** Phase 5.1 contracts.
