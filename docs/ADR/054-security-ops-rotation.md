# ADR-054: Security operations and key rotation readiness

**Status:** Accepted  
**Decision:** Docs SECURITY-OPERATIONS.md: secrets rotation checklist (Supabase, Stripe, OpenAI, APNs/FCM); debug allowlist (DEBUG_* + ALLOW_DEBUG_HOSTS); optional signed admin actions (document/scaffold); security events stream for suspicious actions.

**Context:** Phase 6.8; zero-trust hardening.
