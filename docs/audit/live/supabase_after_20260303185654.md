# Supabase live snapshot — AFTER hardening

**Timestamp:** 20260303185654  
**Project ref:** vthfrxehrursfloevnlp (AISTROYKA)

---

## Validation results

| Check | Result |
|-------|--------|
| Admin views: anon/authenticated have SELECT | **PASS** — all false |
| job_sweep_log RLS enabled | **PASS** — relrowsecurity true |
| job_sweep_log: anon/auth SELECT | **PASS** — revoked (RLS + REVOKE ALL) |
| SECURITY DEFINER: anon/authenticated EXECUTE | **PASS** — all false (REVOKE FROM PUBLIC + GRANT TO service_role) |
| service_role: EXECUTE on definer functions | **PASS** — true |

---

## Changes applied

1. **Views:** REVOKE SELECT on all 25 admin/observability views FROM anon, authenticated.
2. **job_sweep_log:** ENABLE ROW LEVEL SECURITY; REVOKE ALL FROM anon, authenticated; GRANT SELECT, INSERT TO service_role.
3. **SECURITY DEFINER functions:** REVOKE EXECUTE FROM PUBLIC; GRANT EXECUTE TO service_role (for all 22 definer functions in public).

---

*Snapshot after apply.*
