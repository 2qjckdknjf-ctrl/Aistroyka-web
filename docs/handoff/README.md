# docs/handoff/

One file per finished or paused task, created from `docs/ops/HANDOFF_TEMPLATE.md`.

Naming: `<yyyy-mm-dd>-<short-topic>.md`.

A handoff is the bridge between a phone session and a desktop session (or between two agents). `STATUS.md` should link to the latest handoff.
