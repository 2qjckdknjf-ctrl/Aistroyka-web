/**
 * Canonical severity for findings, blockers, and decision reasons.
 * Replaces duplicated BlockerSeverity and inline finding severities.
 */
export type RomaSeverity = "critical" | "warning" | "information" | "unknown";
