//
//  ManagerSemanticColors.swift
//  AiStroykaManager
//

import SwiftUI

enum ManagerSemanticColors {
    static let pageBackground = Color(.systemGroupedBackground)
    static let surfaceMuted = Color(.secondarySystemGroupedBackground)
    static let filterSelected = Color.accentColor
    static let filterUnselected = Color(.tertiarySystemFill)
    static let primary = Color.accentColor
    static let onPrimary = Color.white
    static let primaryDisabled = Color.gray
    static let success = Color.green
    static let warning = Color.orange
    static let error = Color.red
    static let info = Color.blue
    static let accent = Color.accentColor
}
