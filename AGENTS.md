## Learned User Preferences

- Do not use destructive git operations: no force push, no reset --hard, no history rewrite; do not push directly to `main`—use the protected PR merge path. When local `main` is already checked out in another worktree, branch from `origin/main` in a new worktree instead of force-switching the occupied checkout. Canonical clean main worktree for deploy observation, pilot/feature code PRs, and desktop work: `/Users/alex/Projects/AISTROYKA-main-clean`; use primary `/Users/alex/Projects/AISTROYKA` for docs-only branches (e.g. `docs/development-os`); leave `/Users/alex/Projects/AISTROYKA-release-closure` untouched (salvage-only, not for normal git ops). Git worktrees do not share gitignored local assets—copy needed signing/env files from the primary checkout `/Users/alex/Projects/AISTROYKA` into main-clean before store gate checks or upload prep (e.g. root `.env.local`, `ios/Config/Secrets.xcconfig`, `android/keystore.properties`, `android/.secrets/upload-keystore.jks`); never commit them.
- Do not delete or remove things without explicit necessity. Stale remote branch deletion follows dry-run manifest → owner approval → annotated archive tags `archive/<branch>/<date>` before delete → docs-only execution report PR with non-author APPROVED merge; never delete open-PR, dangerous, manual-review, or `cursor/aistroyka-system-maturity-7957` branches. Local branch/worktree archival is the same owner-gated, dry-run-first flow done in small slices: use `git branch -d` (never `-D`) and `git worktree remove` only on clean worktrees, limited to branches merged into `origin/main` AND already archive-tagged AND clean; never touch dirty worktrees, `[ahead N]` branches, or the never-touch prefixes `main`/`release/*`/`ai/*`/`design/*`/`mobile/*`/`security/*`/`cursor/*`/`claude/*`/`snapshots/*`; if unsure, classify `KEEP_REVIEW`.
- Run an audit or check state before performing risky or irreversible actions; for idempotent operator reruns (e.g. branch archival), verify prior execution completed—target branches absent, archive tags present—before re-deleting or re-tagging; prefer running automated tests for the code paths you change unless the task calls for a broader suite. Before new AI feature work, inventory/validate the existing module (hard audit) and do not expand AI scope until an audit verdict exists. Before provisioning external infrastructure (for example Supabase), verify and continue in the active production project **AISTROYKA** (ref `vthfrxehrursfloevnlp`, eu-central-1) per `docs/audit/LIVE_SUPABASE_SCHEMA_REPORT.md`—do not create parallel Supabase projects. Paused **`aistroyka-release1`** is legacy/cold-backup, not the live runtime DB, unless the user explicitly switches.
- Do not break existing dashboard, auth flows, middleware, tenant logic, or platform-owner access gates when adding features; keep a clear public path into the dashboard when the product exposes a cabinet entry, and avoid unexplained post-auth redirects to subscribe or billing when users expect the locale dashboard (pilot and test access should not feel like the cabinet vanished). On public mobile, Cabinet/Кабинет must be visible without relying only on the burger menu (header CTA plus menu). After signup/login, prefer dashboard onboarding over forced `/subscribe` when the user expects the cabinet.
- Do not commit secrets, .env files with real values, tokens, build artifacts, or real pilot client PII; use .gitignore and example files (gitignored `docs/launch/pilot-intake.real.local.json` for real intake—synthetic rehearsal uses `example.com` only). In reports and logs never print secret env values, JWTs, passwords, service-role keys, provider keys, device/push tokens, or raw project/tenant/user IDs—use only `PRESENT`/`MISSING`/`MATCH`/`MISMATCH`. For protected PR merges use `GITHUB_REVIEWER_TOKEN` from root `.env.local` (non-author collaborator PAT with Pull requests write)—never self-approve or bypass branch protection; verify `reviewDecision: APPROVED` via GitHub API before merge. Dual-identity rule: when PR author is `6262265-cpu`, approve via keyring gh as `2qjckdknjf-ctrl` (unset `GH_TOKEN`); when author is `2qjckdknjf-ctrl`, use `GITHUB_REVIEWER_TOKEN` (`6262265-cpu`).
- Prefer new work as isolated additions (components, routes, docs) over broad refactors; put reports and documentation under docs/ and phase subdirs (e.g. docs/mobile-rebuild/, docs/audit/, docs/reconciliation/issue-<N>-<topic>-stacked-audit/ for post-baseline audit-only PRs). Do not rewrite historical dated audits, baselines, or route matrices to backdate fixes—keep them as snapshots and put current proof in separate closure artifacts; if a historical file was wrongly mutated, restore it exactly rather than mass-reformatting. Keep ROMA QA Framework docs (`docs/roma/`, branch `feature/roma-qa-framework`), operational ROMA program (`security/platform-admin-separation`—Operations Center, `@aistroyka/roma-kernel`, `docs/architecture/ROMA_OS_*`, platform-admin audits in `docs/audits/PLATFORM_ADMIN_*`), and the QA platform scaffold (`docs/qa/`, `scripts/qa/`, `apps/web/tests/qa/`) in separate commits/branches unless explicitly decided. Operations Center (owner-facing name for `/platform-admin/testing`; legacy `RomaQa*` prefixes in code) is a read-only live quality dashboard (13-item grouped nav in 5 groups; Dashboard + domain modules)—Safe Readonly Audit V1 allows manual refresh and optional owner Save Snapshot only (no auto-save, scheduled jobs, or catalog test execution); no Run buttons, CI triggers, or production mutations unless explicitly scoped; ROMA refinement-only polish/stabilization sprints forbid new APIs, routes, DB tables, modules, or architecture expansion; platform-admin client code must not pull server-only imports into the client bundle (split shared literals into client-safe `*.constants.ts`—`node:crypto` chains break OpenNext/cf:build); never weaken Cloudflare Access, `platform_owner_grants`, or allow tenant admins on platform-admin surfaces. Do not broad-merge old Liquid Glass/design branches (`design/liquid-glass-public-shell-lg2a`, `design/mobile-liquid-glass`, `feature/unified-product-design-certification`) without fresh rebase and an explicit small-slice plan. Re-slice Liquid Glass by checking out individual files from `origin/release/web-pilot-rc` (file checkout only, never a broad branch merge), and strip banned public-marketing tokens (`MOCK_METRICS`, `500+`/`12K+`, `Request demo`/`Book demo`, `TODO LG`) from sliced files.
- Keep dashboard and public UI copy aligned with the active page locale (next-intl) and consistent product terminology; default to Russian when the user requests Russian (including project audits and comparisons); when changing user-visible strings, update en/ru/es/it message bundles and matching Android/iOS resources together; public primary CTA copy prefers pilot-first wording (Launch pilot → Contact us → Get presentation)—leave Cabinet/login CTAs unchanged; after dashboard or activation onboarding copy changes, run `bun run i18n:check` (scopes `activation.*`/`dashboard.*`/`dashboardDetail.*` vs `en.json`; set `I18N_CHECK_ALL=1` only when intentionally auditing entire locale trees).
- For mobile: do not merge Manager and Worker into one app; keep shared logic in Shared; do not use WorkerLite as primary product name; treat iOS as the primary mobile product contour and defer broad Android parity work until iOS is product-ready (no preemptive Android scope expansion or speculative backend modules for mobile). Native Manager/Worker (iOS + Android) must share the website/cabinet brand system—use live web design tokens (`apps/web` brand CSS/`lib/design`, `public/brand`) as the visual source of truth; Manager and Worker may differ in density/workflow but must look like one product family; do not invent new brand marks. When another task already owns simulators/devices, use isolated DerivedData/AVD/ports and never erase, shut down, or reconfigure those devices; pass an explicit simulator UDID and do not silently pick a busy device. P3 closed: Android deferred for first pilot—not a launch blocker unless owner/client explicitly requires Android Worker MVP; do not claim Android pilot-ready, Google Play available, iOS/Android full parity, or live FCM proven while deferred (formal owner signature may stay OPEN without inventing a blocker for the active defer policy). Prefer Worker field UX/logic audits before feature expansion—keep clear primary paths for photo, comment, and voice/notes. Task Chat is Worker↔Manager per-task messaging with text, WhatsApp-style voice notes (not VoIP calls), photo, and video; chat must be reachable from task surfaces, not a one-off screen. For mobile store MODE B uploads, when the user says "go" first run a read-only gate verification in the same shell/session that will execute the upload before proceeding—never upload directly; do not open duplicate `SAFE_NO_UPLOAD`/`BLOCKED` evidence PRs when the missing-gate state is unchanged. Mobile pilot distribution `READY_FOR_DEVICE_SMOKE` means store builds are VALID and web deploy is current; classify physical device smoke as `DEVICE_SMOKE_PARTIAL` when no device is connected or smoke is incomplete—distribution-ready is not device-smoke-complete. Do not treat an obsolete TestFlight/store build that predates the feature under test as valid device-smoke evidence.
- Do not invent or fake success (e.g. fake build results, UI-only flows without real backend/API behavior, or AI LIVE/LEVEL 4 without `bash scripts/smoke/ai_live_provider.sh --require-live` exit 0); do not claim production GA or that latest `main` is deployed without deployment/`buildStamp` evidence; trust deployed runtime/code over stale docs/prior reports and reality-check that production/uploaded builds actually reflect the intended branch (production or a TestFlight build can lag merged `main` or design branches); document real blockers and missing values. Do not count skipped browser/E2E tests as proof; when required QA/E2E credentials are missing, mark real authenticated E2E `BLOCKED_EXTERNAL` and continue only local contract work; one incomplete `E2E_EMAIL`/account must not be reused as multiple personas. Public homepage must not present synthetic/mock production-scale metrics (no fake numeric counts like `500+`/`12K+`—prefer qualitative capability statements; the `MOCK_METRICS` numbers were removed). Do not use obsolete Python `backend/` live gates; copilot SSE `event: done` or vision HTTP 200 alone is not live LLM proof—require absence of `X-AI-Fallback-Reason` / non-fallback response per `docs/ai/AUDIT_AI_VALIDATION_REPORT.md`.
- When config values may exist in repo: search first (env examples, docs, scripts); do not ask for values that are already there; do not hardcode fake placeholders if real values exist.
- Prefer end-to-end execution when asked ("do it yourself"); on continue signals («делай», keep going, по плану, etc.) chain focused work: validate, fix failures, proceed—no extra checkpoints unless blocked or unclear scope. Preserve the dirty worktree and do not revert unrelated local changes. On bounded phase/batch work, do not apply migrations, commit, push, or deploy unless explicitly asked; do not start the next batch while known in-scope tails remain—claim closure only with an honest YES/NO. Owner-authorized temporary remote fixtures (auth users, memberships, projects) for authenticated E2E require exact post-run cleanup; use service role only for fixture bootstrap/read/cleanup—not for app-path mutations (report/upload/submit/review). Never send live push to synthetic, random, or DB-scraped device tokens, and do not drain a shared push outbox. When subagent output is already shown to the user, do not restate it unless asked or multi-result synthesis is required. For continual-learning runs, process only parent transcripts (`UUID/UUID.jsonl`) with incremental mtime-based indexing.
- Product sequencing and customer-finance boundaries: follow `docs/roadmap/AISTROYKA_MEGA_ROADMAP_CUSTOMER_FINANCE_SAFE.md`; never expose internal contractor financial state on customer/owner/stakeholder surfaces except designed customer-facing commercial artifacts (see `docs/security/*` isolation audits). The additive account layer / billing migration is staged and gated: keep production billing behavior identical, and do not switch billing reads or enable account-first entitlement resolution (`ENTITLEMENT_RESOLUTION_SOURCE`) until operational gates pass (7-day shadow validation with zero drift, staging runtime proof, production cutover)—do not bypass these gates or mark a stage closed while they are open. When hardening shared API helpers (idempotency, rate limits, tenant resolution), do not weaken legacy or sibling routes—prefer strict opt-in/atomic helpers with compatible tests.

## Learned Workspace Facts

- Aistroyka is a monorepo; web application lives in apps/web (Next.js, App Router).
- Root build: from repo root run `bun install --frozen-lockfile` (Bun **1.2.15**, see `.tool-versions`) and `bun run build` (builds packages/contracts then apps/web). Canonical lock: root `bun.lock` only; root `package-lock.json` is for Vercel npm preview — validate with `node scripts/ci/validate-npm-lock.cjs`; do not commit `package-lock.json` on bun-only changes. Security headers: shared source `apps/web/lib/security-headers.ts`; middleware applies page/API profiles, and narrow non-overlapping `next.config.js` `headers()` cover middleware-excluded static/`_next`/favicon paths (avoid dual ownership / comma-joined duplicates); smoke `bash scripts/smoke/security_headers.sh`. Do not resolve production advisories with `npm audit fix --force` or ignore/allowlist suppression.
- Production runtime and DNS ownership is **Cloudflare Workers** (`apps/web/wrangler.toml`, OpenNext); use `bun run cf:build` then Wrangler deploy, and verify apex/www routes through Cloudflare when making production readiness claims. Worker custom-domain/route writes may need Wrangler OAuth (`unset CLOUDFLARE_API_TOKEN`) when the repo API token is read-only; Cloudflare Access on `admin.aistroyka.ai` uses `CLOUDFLARE_ACCESS_API_TOKEN` exclusively (never legacy `CLOUDFLARE_API_TOKEN` for Access)—read-only audit `apps/web/scripts/cf-admin-domain-access-audit.mjs`; Worker route writes may still need Wrangler OAuth when the repo API token is read-only. Public abuse controls trust `cf-connecting-ip` only when `AISTROYKA_TRUST_CF_CONNECTING_IP=1` on the canonical Worker ingress—do not treat client-supplied `x-forwarded-for` / `x-real-ip` as a security dimension. **Canonical production domain is `aistroyka.ai`** (including `www.aistroyka.ai`); staging is **`staging.aistroyka.ai`**—confirm deploy via `GET /api/v1/health` → `buildStamp.sha7` on **staging** or **production** (both may expose `buildStamp` post-deploy; if absent on production, use staging health or GitHub Actions Deploy Cloudflare workflows for SHA proof). `aistroyka.com` and `www.aistroyka.com` are redirect-only entry domains (301 to `https://aistroyka.ai`, preserving path and query)—do not serve the app independently from `.com`. Do not run `bun run build` and `bun run cf:build` in parallel—concurrent Next/OpenNext work can corrupt `apps/web/.next`; run sequentially or remove `apps/web/.next` before retrying. Web UI is App Router under `apps/web/app` (no `pages/` tree); root-level error UI uses `app/global-error.tsx`—reintroducing `pages/*` (for example a custom 500 page) can destabilize standalone or OpenNext builds, so validate with `cf:build` before relying on it. **AI runtime is `apps/web` only** (no repo Python `backend/`); canonical live gate `bash scripts/smoke/ai_live_provider.sh --require-live`; supplementary smokes `scripts/smoke/ai_*.sh`; architecture truth in `docs/ai/AUDIT_AI_RUNTIME_ARCHITECTURE_TRUTH.md`. Health `aiConfigured` means `AI_ANALYSIS_URL` is set; `openaiConfigured` means `OPENAI_API_KEY`—do not treat openaiConfigured alone as analysis-pipeline readiness. User HTTP analysis process (`POST /api/v1/analysis/process` and legacy `/api/analysis/process` via `lib/ai/analysis-process.http.ts`) must use tenant-scoped `dequeue_tenant_job` only; global `dequeue_job` remains for trusted background workers.
- iOS: AiStroykaManager and AiStroykaWorker (shared in `ios/Shared`); checked-in Xcode projects include **UITest** targets (`WorkerSmokeUITests`, `ManagerSmokeUITests`) for simulator smoke (login + `pilot_*` ids); Layer B live pilot E2E uses gitignored `ios/Config/.uitest-e2e-credentials` via `AISTROYKA_E2E_CRED_FILE`—do not bundle E2E credentials in SPM Shared resources; PRs that touch `ios/` (excluding **markdown-only** changes under `ios/**/*.md` for the UITest workflow) run `.github/workflows/ios-ui-smoke.yml`; use **workflow_dispatch** for a manual run; locally run `ios/scripts/run-ios-uitest-smoke-local.sh`, `ios/scripts/run-ios-e2e-integration-local.sh`, or `ios/scripts/ci-pick-iphone-simulator-udid.sh` + `xcodebuild test` (see `ios/README.md`). Android apps now exist on `main` (Manager + Worker Compose scaffolds under `android/`, shared in `android/shared`, thinner than iOS; Android Worker has FCM + an instrumented launch test); iOS WorkerLite→Worker rename is complete on `main`; mobile pilot readiness is tracked in `docs/release-hardening/MOBILE_PILOT_READINESS.md`; the mobile pilot release-candidate source of truth is branch `release/mobile-pilot-rc` (do not build pilot binaries from `main`, `feature/unified-product-design-certification`, or stale stage branches). iOS Layer B live E2E runs against staging (`IOS_E2E_BASE_URL=https://staging.aistroyka.ai`, often with `CI_SIGNING_HACK=1`) via `ios/scripts/run-ios-e2e-integration-local.sh`. Android release signing/Play uploads use gitignored `android/.secrets/upload-keystore.jks`, `android/.secrets/google-play-service-account.json`, and `android/keystore.properties` (release `signingConfigs.release` reads `storeFile`/`storePassword`/`keyAlias`/`keyPassword` from `keystore.properties`); never commit them—`.gitignore` also covers `android/.secrets`, `*.jks`, `*.aab`, `*.apk`. Android Play `targetSdk`/`compileSdk` 35 (Play target-API requirement) needs AGP 8.6.1 + Gradle 8.7 + JDK 17, and release `versionCode` can be overridden via the `AISTROYKA_ANDROID_VERSION_CODE` Gradle property / env var (local builds keep a small integer). No TestFlight/App Store/Google Play/pilot-live claims without verified build/deployment evidence (iOS build/UITest needs Xcode/simulator; Android build/UITest needs Android SDK/Gradle). WorkerLite is deprecated as the primary product name. iOS App Store Connect identifiers: bundle IDs `ai.aistroyka.worker` (SKU `aistroyka-worker`) and `ai.aistroyka.manager` under Apple Team `43A4KW5BKB`; the App Store Connect API key is a gitignored `.p8` AuthKey, and store uploads require pre-created ASC app records plus an Apple Distribution certificate (no store-live claims without verified upload evidence). The mobile **build/runtime audit is complete and closed** (that closure implies no store readiness); store/distribution is split into separate owner-gated tracking work (iOS TestFlight/App Store, Android Google Play, and a consolidated mobile pilot distribution decision that defaults to **NO-GO** until both stores clear). iOS TestFlight upload is the symmetric Mode B gate behind `APPROVE_TESTFLIGHT_UPLOAD=YES` plus `AISTROYKA_IOS_BUILD_NUMBER`, an owner-provided Apple Distribution certificate, App Store provisioning, ASC API access, and an `ExportOptions.plist`; until those exist iOS distribution stays `OWNER_ACTION_REQUIRED`. Android Google Play upload is gated behind `APPROVE_GOOGLE_PLAY_UPLOAD=YES` plus a verified Play Console record and service-account credentials (Mode B)—local signed AAB readiness can be READY while Google Play upload remains `OWNER_ACTION_REQUIRED`. When owner upload gates are missing, the Mode B step still produces a sanitized docs-only `SAFE_NO_UPLOAD` evidence PR under `docs/reconciliation/issue-<N>-*-mode-b-*`; owner-action checklists for store prerequisites live under `docs/reconciliation/issue-<N>-*-owner-actions-*`. MODE B upload env contract: Android needs `APPROVE_GOOGLE_PLAY_UPLOAD=YES` + `AISTROYKA_ANDROID_VERSION_CODE` + `GOOGLE_PLAY_SERVICE_ACCOUNT_FILE` (Play JSON with publish permission), with `androidpublisher.googleapis.com` enabled on the GCP project and the JSON `client_email` (service account, not a personal Google login) granted in Play Console; iOS needs `APPROVE_TESTFLIGHT_UPLOAD=YES` + `AISTROYKA_IOS_BUILD_NUMBER` + `APP_STORE_CONNECT_API_KEY_PATH`/`APP_STORE_CONNECT_API_KEY_ID`/`APP_STORE_CONNECT_API_ISSUER_ID` + installed Apple Distribution identity + App Store provisioning for both bundle IDs + `ios/ExportOptions-AppStore.plist` (method `app-store`, teamID `43A4KW5BKB`, automatic signing). ASC upload validation rejects missing iPad `UIInterfaceOrientationPortraitUpsideDown`, alpha-channel App Store icons (use opaque RGB), and Worker `camera` in `UIRequiredDeviceCapabilities` (ASC 90098/90108—remove `camera` but keep `NSCameraUsageDescription` for optional camera use). `Info.plist` may hardcode `CFBundleVersion`—`AISTROYKA_IOS_BUILD_NUMBER`/`CURRENT_PROJECT_VERSION` alone may not apply; verify archive metadata before export. Non-interactive MODE B export/codesign can fail `errSecInternalComponent` until the login keychain is unlocked and the Apple Distribution key allows codesign partition access. Local MODE B credential/signing assets stage under gitignored `local-secrets/` plus the `.env.local` mobile-store section (credential file paths left commented until the owner provides them); the `.gitignore` "Mobile store MODE B" block excludes `local-secrets/`, `.secrets/`, `*.p8`, `AuthKey_*.p8`, `*.cer`, `*.mobileprovision`, `*.xcarchive`, `*.ipa`, and `*service-account*.json`/`google-play*.json`.
- Local iOS config: ios/Config/Secrets.xcconfig (gitignored) and Secrets.xcconfig.example; both apps use the same xcconfig.
- Public site and dashboard coexist; locale routes under [locale]; public pages under (public), dashboard under (dashboard); web i18n uses next-intl message files for en/ru/es/it.
- API routes live under apps/web/app/api/; treat /api/v1/* as canonical, and do not change tenant/auth logic without necessity. Active tenant selection (`x-tenant-id` / `aistroyka_active_tenant`) is fail-closed: invalid, unauthorized, conflicting, or DB-error explicit claims must never fall back to auto-create workspace or silent tenant switch; browser set/clear is `POST`/`DELETE` `/api/v1/tenant/active` with cookie-auth same-origin CSRF (matching Origin or reliable same-origin `Sec-Fetch-Site`—missing both fails closed). Tenant-admin `/admin/**` navigation and authorization must follow the active tenant—being owner/admin in tenant A must not grant admin UI or admin-scoped reads while tenant B is active and the user is only a member/viewer there; do not assume RLS alone enforces that. Help POST routes (`/api/v1/help/assistant`, `/hints`, `/assistant/events`) are tenant-authenticated; lite idempotency uses strict atomic claim and fails closed when the store is unavailable; rate-limit denial is fail-closed. Mobile sync 409 conflict responses expose `serverCursor` (a `server_cursor` alias may appear) for reconciliation—follow docs/runbooks/MOBILE_SYNC.md. Field-worker `x-client` profiles `ios_lite`, `android_lite`, `ios_worker`, and `android_worker` are restricted by a segment-safe allow-list in Edge `middleware.ts` (`apps/web/lib/api/lite-allow-list.ts`); disallowed `/api/v1/*` paths return 403. Legacy non-v1 handlers (e.g. `/api/projects*`, `/api/ai/*`) must also fail closed for those profiles at the route handler—middleware or v1 redirects alone are insufficient because Cloudflare Worker builds may bypass Next middleware for most `/api/v1/*`. An additive `accounts` layer sits above `tenants` (`accounts` table + `tenants.account_type`, three account types platform/contractor/client, project modes `contractor_led_project`/`client_led_project`, account-scoped membership and billing); `account_subscriptions` is the account billing source of truth while `billing_customers`/`entitlements` are dual-written with `account_id` (keeping `tenant_id` as the Stripe webhook anchor), and a suspended account (`accounts.status = suspended`) fails closed across all its tenants. RBAC route topology: `/dashboard` = contractor ops, `/admin` = tenant company admin only (no platform billing/leads/global cron), `/platform-admin` = platform owner cabinet (canonical UI; `/owner` redirects here; preferred host `admin.aistroyka.ai` is live with DNS/TLS on the production Worker and Cloudflare Access enabled—owner-only email allow, MFA, no bypass; `OWNER_ALLOWED_HOSTS` host enforcement deferred until post-Access Phase 2; safe fallback `/[locale]/platform-admin`); Platform Admin auth is defense-in-depth: Access perimeter → Supabase session → `platform_owner_grants` (`tenant_members` owner/admin is not sufficient); Access identity may differ from Supabase login email; verify live `platform_owner_grants` exists before admin-domain go-live; unauthenticated platform-admin pages redirect to login (307), layout guard must not `forbidden()` on missing session (OpenNext 500); `/portal` = invited stakeholder portal; platform APIs canonical at `/api/v1/platform/*` (deprecated aliases `/api/v1/owner/*`, `/api/v1/admin/billing|leads/*`; middleware platform-owner gate must cover those billing/leads aliases with segment-safe prefix matching, plus handler-level `requirePlatformOwnerApi`); P0 lockdown—tenant admins cannot POST global flags, trigger cross-tenant cron, or reach platform billing/leads; Operations Center at `/platform-admin/testing` (owner-facing; legacy V1 `/testing/{section}` routes redirect to canonical module paths via `roma-path-domain-rules.ts`) is read-only (live Dashboard probes + engineering intelligence; `executionEnabled: false`)—not test execution or CI; `@aistroyka/roma-kernel` (`packages/roma-kernel/`) is Layer 1 canonical domain types/contracts only—ROMA app modules import shared severity/confidence/release types from kernel; safe-audit owner APIs at `/api/v1/platform/testing/safe-audit/{refresh,save,runs}`; persisted run history in `public.roma_audit_runs` (RLS enabled, zero policies, service-role insert only; list API excludes `raw_payload_redacted`); admin-domain rollout docs under `docs/security/ADMIN_DOMAIN_*` and owner-only access report `docs/security/PLATFORM_ADMIN_OWNER_ONLY_ACCESS_REPORT.md` with automation `apps/web/scripts/cf-admin-domain-*.mjs`; `tenant_id` stays the operational RLS isolation boundary; stakeholders are NOT `account_members`; `project_members` reads/writes are scoped via `can_read_project_membership`/`can_manage_project_membership`; platform owner client-data access goes through audited `platform_break_glass_grants` (RLS); stakeholder middleware gates must fail closed. Additive account-billing tables `account_billing_customers`/`account_entitlements`/`tenant_entitlement_projections` feed a resolver (`resolveEffectiveEntitlements`, `ENTITLEMENT_RESOLUTION_SOURCE` = legacy/shadow/account) with drift tracked in `entitlement_resolution_drift` (RLS, service-role only) and parity checked via `billing_entitlement_parity.mjs`. RBAC/account/billing design + audit docs live under `docs/architecture/` (`RBAC_*`, `ACCOUNT_*`, `BILLING_*`, `ROMA_OS_*`).
- Docs and phase reports go under docs/ and subdirs (e.g. docs/audit/, docs/final/, docs/mobile-rebuild/, docs/deploy-fix/, docs/pilot-launch/, docs/reconciliation/ including `branch-archival-dry-run-*`, `branch-archival-execution-*`, and `issue-<N>-stale-branch-archival-*` plan/audit trees). The repo also has a cross-surface "project operating system" doc layer for desktop + mobile/cloud-agent continuation: `PROJECT_CONTEXT.md` (master context, safe to show any agent, no secrets), `STATUS.md` (trusted active task/branch pointer), and `docs/ops/` (`WORKING_MODEL.md`, `CLOUD_AGENT_WORKFLOW.md`, `TASK_TEMPLATE.md`, `HANDOFF_TEMPLATE.md`, `VALIDATION_CHECKLIST.md`, `REPOSITORY_OPERATING_INVENTORY.md`, `SCRIPTS_INVENTORY.md`, `BRANCH_ARCHIVAL_POLICY.md`) plus setup/dry-run reports under `docs/reports/`. Keep `docs/CURRENT_PROJECT_TRUTH_INDEX.md` updated after significant merged PRs/issues close. Canonical full-project audit on main: `docs/audit/AUDIT_2026-06-11_full-project-audit-v2.md`. Phase 13 roadmap closure verdict: **CONDITIONAL YES** in `docs/product/PHASE13_ROADMAP_CLOSURE.md` — the mega-roadmap defines no separate Phase 14 product scope after Phase 13 (section 14 is the execution template; §15 is the strategic verdict). Live/staging smoke policy: `docs/ops/LIVE_STAGING_SMOKE_RUNBOOK.md`. Pilot audit: `bun run audit:pilot` at repo root; preflight env for pilot smoke with `bun run smoke:pilot:check` (use `--strict` to fail when `ops/metrics` auth is missing); Playwright specs live under `apps/web/tests/e2e` with `PLAYWRIGHT_BASE_URL` as baseURL; scoped pilot run `bun run --cwd apps/web e2e:pilot`; env template `.env.pilot.example`; local strict smoke runs often require loading both `/.env.pilot` and `apps/web/.env.local` plus aligning `BASE_URL`/`PLAYWRIGHT_BASE_URL` to active local app URL (`NEXT_PUBLIC_APP_URL`, often `localhost:3001` when `3000` is busy); artifacts under docs/audit/artifacts/. P2 pilot packaging: closure/readiness docs under `docs/pilot/P2_*`, operator scripts under `scripts/pilot/*` (dataset setup, role smoke); staging-first with production blocked unless `PILOT_ALLOW_PRODUCTION=YES`. P3/P4 closed: Android deferred for first pilot; pilot launch ops under `docs/launch/` with `PILOT_INTAKE_CARD.md`, `pilot-intake.template.json`, gitignored `pilot-intake.real.local.json`, and `node scripts/pilot/validate_pilot_intake.mjs`—real client Day 0 stays NO-GO until intake is READY. ROMA spec docs under `docs/roma/` (`feature/roma-qa-framework`); operational ROMA/Operations Center work and ROMA OS runtime architecture under `docs/architecture/ROMA_OS_*` on `security/platform-admin-separation` (RC certification ~9.2/10, pilot-ready—not enterprise-complete); separate from `docs/qa/` Playwright scaffold. Guests may redirect to `/[locale]/login?next=` (not only the bare locale root); dashboard audit specs that exercise auth expect `E2E_USER_EMAIL`/`E2E_USER_PASSWORD` (or legacy `SMOKE_*`).
- Environment variables for production are documented in docs/ENVIRONMENT-VARIABLES.md; required: NEXT_PUBLIC_SUPABASE_URL, NEXT_PUBLIC_SUPABASE_ANON_KEY, NEXT_PUBLIC_APP_URL. For Cloudflare/OpenNext (`cf:build`), those `NEXT_PUBLIC_*` values must be present at build time—they are inlined into the client bundle, so Worker runtime secrets alone do not satisfy client-side checks (e.g. login). Apple/Telegram provider buttons on login/register only render when configured (Supabase Apple provider; `NEXT_PUBLIC_TELEGRAM_BOT_USERNAME` or `TELEGRAM_BOT_USERNAME` plus `TELEGRAM_BOT_TOKEN`). Public Proof Pack token route (`GET /api/v1/share/proof/:token`) needs `SUPABASE_SERVICE_ROLE_KEY` server-side so the app can resolve share rows and pack sources under RLS; without it the route returns 503. Local full test runs with `SUPABASE_SERVICE_ROLE_KEY` in `.env.local` may hit real admin clients unless tests mock `@/lib/supabase/admin` `getAdminClient` (same pattern as `ops/metrics/route.test.ts`).
- Supabase migrations live under `apps/web/supabase/migrations/`; apply to active project **AISTROYKA** (`vthfrxehrursfloevnlp`, eu-central-1) via Supabase CLI / dashboard (the repo previously shipped `apply-migrations.yml` — removed in Release 1 cleanup; re-add if you need GitHub-driven apply). MCP `apply_migration` records remote versions with a different timestamp than the repo filename, so timestamp skew between repo and live migration history accumulates—reconcile repo↔remote migration history (object/SQL equivalence, never destructive) before relying on Supabase CLI push/diff workflows. Punch list / defects in-app are modeled as `public.project_defects` (mega-roadmap text may still say `punch_items`). Auth leaked-password (HIBP): enable via Management API with `apps/web/scripts/enable-auth-leaked-password-protection.mjs` or GitHub workflow **Supabase Auth HIBP enable** when Dashboard UI is unavailable (`SUPABASE_ACCESS_TOKEN`).
- PR merge gate: GitHub **CI Check** (`.github/workflows/ci-check.yml`) runs `bun install`, `bun run i18n:check`, lint, tests, and `cf:build` on each pull request (`i18n:check` uses `scripts/i18n/check-messages.js`). Web lint is ESLint CLI on `app components lib middleware.ts` (not `next lint`); `apps/web/.eslintrc.json` needs `"root": true` or the monorepo root ESLint config ignores `apps/*` and lint silently no-ops. Vitest node tests (`**/*.test.ts`): extract pure helpers to `.ts` when imports would load JSX components (e.g. `AISignalLine.helpers.ts`) to avoid Rolldown JSX parse failures. `main` branch protection requires `enforce_admins: true`, one non-author `APPROVED` review, and passing `check`—do not merge without formal approval. When `/usr/local/bin/gh` fails with bad CPU type on Apple Silicon, use `~/.local/gh-arm64/gh_2.95.0_macOS_arm64/bin/gh`. Deployment ownership / duplicate Vercel noise vs canonical paths: see `docs/runbooks/DEPLOYMENT_SOURCE_OF_TRUTH.md` and `docs/incidents/DEPLOY_TOPOLOGY_RCA_AND_FIX.md`. Production deploys via the canonical CI chain (merge to `main` → **Deploy Cloudflare (Staging)** → verify staging → **Deploy Cloudflare (Production)** via `workflow_run`), not local `wrangler` when no Cloudflare creds are present; `buildStamp.sha7` is the first 7 chars of the deployed commit SHA.
- Staging deploy (`.github/workflows/deploy-cloudflare-staging.yml`) may call reusable **Pilot E2E audit** (`.github/workflows/pilot-e2e-audit.yml`) after `pilot-smoke` with `continue-on-error: true`; that workflow supports `workflow_call` (and `workflow_dispatch`) with optional `locale` input (default `en`, maps to `E2E_LOCALE`); requires repo secrets `PILOT_E2E_BASE_URL`, `PILOT_E2E_EMAIL`, `PILOT_E2E_PASSWORD` (optional `PILOT_E2E_PROJECT_ID`).
