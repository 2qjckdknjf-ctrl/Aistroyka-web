# Cursor / IDE
.cursor/
.DS_Store

# dependencies
node_modules
node_modules/
.pnp
.pnp.js

# Maestro pilot logs (local)
maestro/output/

# Google Drive Desktop sync temp fragments (do not commit)
.tmp.driveupload/
.tmp-home/

# testing
coverage
TestLogs/
test-results/
playwright-report/
apps/web/test-results/

# iOS derived builds
ios/build/
ios/**/build/
ios/ci_derived/
ios/**/ci_derived/
ios/.deriveddata_*/
ios/**/DerivedData/
DerivedData/
**/DerivedData/
**/SourcePackages/
# SwiftPM local build tree (must not ship; historically some paths were tracked)
ios/Shared/.build/
ios/**/.build/
**/.build/
*.xcresult
ios/Config/Secrets.xcconfig
ios/Config/.uitest-e2e-credentials
ios/Shared/Sources/Shared/e2e-credentials.env
**/xcuserdata/
*.xcuserstate

# Android build output / signing materials (never commit secrets or artifacts)
android/build/
android/**/build/
android/.secrets/
android/keystore.properties
**/*.jks
**/*.keystore
*.aab
*.apk

# archives / exports
*.zip
archive_*/
exports/

# large audit artifacts (root-only; do not ignore app/api/.../reports/ or lib/domain/reports/)
audit_artifacts/
audit_artifacts_*/
artifacts/
/reports/
audit_*/
engine/
# iOS: ignore derived/build only; commit ios/WorkerLite source
bun.lock.bak

# next
.next
out

# workspace packages build output
packages/contracts/dist
packages/roma-kernel/dist

# macOS
.DS_Store

# misc (keep .DS_Store above for visibility)
*.pem
*.key
*.p12

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
build-server-ts-head.log

# env
.env
.env.local
.env.e2e
apps/web/.env.local

# local Supabase (no secrets committed)
supabase/.temp
supabase/.env
apps/web/supabase/.temp
apps/web/supabase/.env
.env.development.local
.env.test.local
.env.production.local

# vercel
.vercel

# cloudflare / opennext
.open-next
.dev.vars

# duplicate copies (avoid TS picking untyped adapter)
lib/supabase/server 2.ts
lib/supabase/middleware 2.ts

# typescript
*.tsbuildinfo
next-env.d.ts

# DevOps backup list (extra exclusions)
.env*
!.env.e2e.example
!.env.pilot.example
.env.pilot
apps/web/tests/e2e/_state/
.idea
.vscode
tmp
*.sqlite
secrets
secrets/
uploads/
.wrangler
.turbo
*.log

# Mobile store MODE B — local credentials and upload artifacts (never commit)
local-secrets/
# Root-only upload evidence dumps (do not ignore packages/*/src/evidence)
/evidence/
.secrets/
*.p8
AuthKey_*.p8
*.cer
*.mobileprovision
*.xcarchive
*.ipa
play-service-account*.json
google-play*.json
service-account*.json

# Local agent evidence (screenshots/logs/deriveddata)
.evidence/
